document.addEventListener("DOMContentLoaded", () => {
  let node  = document.querySelector("#copy")
  let t = node.innerText
  node.innerText += t
  node.innerText += t
  node.innerText += t
  node.innerText += t
  node.innerText += t
  node.innerText += t
  node.innerText += t
  node.innerText += t
  node.innerText += t
  node.innerText += t
  node.innerText += t
  node.innerText += t
  node.innerText += t
  node.innerText += t
})
